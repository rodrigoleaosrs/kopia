services:
    kopia:
        image: kopia/kopia:latest
        hostname: ${KOPIA_HOSTNAME}
        container_name: Kopia
        restart: unless-stopped
        command:
            - server
            - start
            - --disable-csrf-token-checks
            - --insecure
            - --address=0.0.0.0:51515
            - --server-username=${KOPIA_SERVER_USERNAME}
            - --server-password=${KOPIA_SERVER_PASSWORD}
        environment:
            # Set repository password
            KOPIA_PASSWORD: "${KOPIA_REPO_PASSWORD}"      #optional
            USER: "${KOPIA_USER}"                         #optional
        volumes:
            # Mount local folders needed by kopia
            - kopia-config:/app/config
            - kopia-cache:/app/cache
            - kopia-logs:/app/logs
            # Mount local folders to snapshot
            - /data:/backup/data:ro
            - /etc/fail2ban:/backup/fail2ban:ro
            - /var/ossec/etc:/backup/wazuh:ro
            - /etc/logrotate.conf:/backup/logrotate.conf:ro
            - /etc/logrotate.d:/backup/logrotate.d:ro
            # Mount repository location
            - kopia-repository:/repository
            # Mount path for browsing mounted snapshots
            - kopia-tmp:/tmp
        labels:
            - "traefik.http.services.kopia.loadbalancer.server.port=51515"
